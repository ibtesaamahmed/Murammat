import 'package:flutter/material.dart';

class CostumerSettingsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Container(child: Text('COSTUMER SETTINGS')));
  }
}
