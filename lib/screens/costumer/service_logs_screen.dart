import 'package:flutter/material.dart';

class ServiceLogsScreen extends StatelessWidget {
  static const routeName = '/service-logs';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Service Logs'),
      ),
    );
  }
}
