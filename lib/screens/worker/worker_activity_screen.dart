import 'package:flutter/material.dart';

class WorkerActivityScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Container(child: Text('WORKER ACTIVITY')));
  }
}
