import 'package:flutter/material.dart';

class WorkerShopLocationScreen extends StatelessWidget {
  const WorkerShopLocationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
