import 'package:flutter/material.dart';

class WorkerSettingsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text('WORKER SETTINGS'),
      ),
    );
  }
}
